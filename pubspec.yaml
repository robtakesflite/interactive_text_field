name: interactive_text_field
description: "A modular, framework-agnostic interactive text field for Flutter with plugin-based syntax highlighting, markdown, slash/mention triggers, and code completion."
version: 0.1.0
homepage: https://github.com/robertmollentze/interactive_text_field
repository: https://github.com/robertmollentze/interactive_text_field
issue_tracker: https://github.com/robertmollentze/interactive_text_field/issues

environment:
  sdk: ^3.11.5
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
